version: '3.8'
services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html # This syncs your local files so you don't have to rebuild Docker every time you change HTML/JS